import 'package:flutter/material.dart';

class MenuFilterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Filter Menu'),
        backgroundColor: Color(0xFF800000),
      ),
      body: Column(
        children: [
          // Add filter options here
        ],
      ),
    );
  }
}
